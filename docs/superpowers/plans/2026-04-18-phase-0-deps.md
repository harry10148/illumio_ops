# Phase 0：相依性基線 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把所有 Phase 1–9 將會用到的 21 個生產套件 + 7 個 dev 套件一次性 pin 到 `requirements.txt` 與 `requirements-dev.txt`，並驗證乾淨 venv 可成功 install、所有 import 可解析、既有 127+ 測試全綠 — 不更動任何業務程式碼。

**Architecture:** 純依賴管理變更。建立 `requirements.txt` (生產) 與 `requirements-dev.txt` (僅開發) 雙檔分離，並新增 `scripts/verify_deps.py` smoke test 腳本確認所有套件可 import。本 Phase 不引入任何 `import` 到業務程式碼，僅在 verify 腳本內 import。

**Tech Stack:** pip + pinned versions only。預期 install 後總大小 ~250 MB（site-packages），但這是開發環境；RPM bundle 體積另外計算。

**Branch:** `upgrade/phase-0-deps`

**完成後 tag:** `v3.4.0-deps`

---

## File Structure

| 檔案 | 動作 | 責任 |
|---|---|---|
| `requirements.txt` | 重寫 | 生產執行需要的 21 套件，全部 pin 主版本範圍 |
| `requirements-dev.txt` | 新增 | 僅開發/測試/打包需要的 7 套件 |
| `scripts/verify_deps.py` | 新增 | 試 import 每個套件並回報版本，失敗即 exit 1 |
| `tests/test_dependency_baseline.py` | 新增 | pytest fixture 跑 verify_deps，列入 CI |
| `Status.md` | 更新 | 標示 Phase 0 完成、版本記為 v3.4.0-deps |
| `Task.md` | 更新 | 新增「Phase 0: dependency baseline」一節並標 ✅ |

**檔案影響面**：6 檔，**0** 個 src 業務檔變更。

---

## Task 1：建立 dev branch 並備份現有 requirements.txt

**Files:**
- Modify: `requirements.txt:1-13`（備份現況到 commit）

- [ ] **Step 1：從 main 開新 branch**

```bash
cd D:/OneDrive/RD/illumio_ops
rtk git checkout main
rtk git pull
rtk git checkout -b upgrade/phase-0-deps
```

Expected: switched to a new branch.

- [ ] **Step 2：確認當前 requirements.txt 內容（基線快照）**

Run: `rtk read requirements.txt`

Expected output（第 1-13 行）：
```
# Core: Zero external dependencies (Python stdlib only)
# CLI and daemon modes work without any pip install.

# Optional: Web GUI mode (--gui)
flask>=3.0,<4.0
... (rest of file)
```

- [ ] **Step 3：commit 起點 — 確保乾淨工作區**

```bash
rtk git status
```

Expected: working tree clean.

---

## Task 2：寫新的 requirements.txt（生產套件）

**Files:**
- Modify: `requirements.txt`（完整重寫）

- [ ] **Step 1：寫入新 requirements.txt 內容**

完整新內容（請完全覆寫）：

```
# illumio_ops production runtime dependencies.
# All packages bundled into the offline RPM via PyInstaller.
# Pinned to major version ranges; minor/patch upgrades allowed within range.
#
# Categories follow the upgrade roadmap phases:
#   Phase 1 — CLI UX:          rich, questionary, click, humanize
#   Phase 2 — HTTP client:     requests, orjson, cachetools
#   Phase 3 — Settings:        pydantic, pydantic-settings
#   Phase 4 — Web GUI security: flask-wtf, flask-limiter, flask-talisman, flask-login, argon2-cffi
#   Phase 5 — Reports:         openpyxl, weasyprint, matplotlib, plotly, pygments
#   Phase 6 — Scheduler:       APScheduler
#   Phase 7 — Logging:         loguru
#   Existing (mandatory):      flask, pandas, pyyaml

# ── Existing core (Web GUI + reports) ────────────────────────────────────────
flask>=3.0,<4.0
pandas>=2.0,<3.0
pyyaml>=6.0,<7.0

# ── Phase 1: CLI UX ──────────────────────────────────────────────────────────
rich>=13.7,<14.0
questionary>=2.0,<3.0
click>=8.1,<9.0
humanize>=4.9,<5.0

# ── Phase 2: HTTP client ─────────────────────────────────────────────────────
requests>=2.31,<3.0
orjson>=3.9,<4.0
cachetools>=5.3,<6.0

# ── Phase 3: Settings validation ─────────────────────────────────────────────
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# ── Phase 4: Web GUI security ────────────────────────────────────────────────
flask-wtf>=1.2,<2.0
flask-limiter>=3.5,<4.0
flask-talisman>=1.1,<2.0
flask-login>=0.6,<0.7
argon2-cffi>=23.1,<25.0

# ── Phase 5: Reports (Excel + PDF + interactive charts) ──────────────────────
openpyxl>=3.1,<4.0
weasyprint>=61.0,<62.0
matplotlib>=3.8,<4.0
plotly>=5.20,<6.0
pygments>=2.17,<3.0

# ── Phase 6: Scheduler ───────────────────────────────────────────────────────
APScheduler>=3.10,<4.0

# ── Phase 7: Logging ─────────────────────────────────────────────────────────
loguru>=0.7,<0.8
```

Use the Write tool to overwrite `requirements.txt` with the above content.

- [ ] **Step 2：commit**

```bash
rtk git add requirements.txt
rtk git commit -m "chore(deps): pin all upgrade roadmap production packages

Adds 18 new packages across Phase 1-7 of the upgrade roadmap, all
pinned to major version ranges. pandas + pyyaml promoted from optional
to mandatory (already used by 41 files / 338 DataFrame ops).

No business code changes; verification in subsequent tasks."
```

---

## Task 3：建立 requirements-dev.txt

**Files:**
- Create: `requirements-dev.txt`

- [ ] **Step 1：建立新檔**

Use the Write tool to create `requirements-dev.txt` with this exact content:

```
# illumio_ops development & testing dependencies.
# NOT bundled into production RPM. Install via:
#   pip install -r requirements.txt -r requirements-dev.txt

# ── Test framework ──────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
responses>=0.25,<1.0          # Phase 2: requests mock
freezegun>=1.4,<2.0           # Phase 6: scheduler time mock

# ── Lint / type ─────────────────────────────────────────────────────────────
ruff>=0.4,<1.0
mypy>=1.10,<2.0

# ── Build / package ─────────────────────────────────────────────────────────
build>=1.2,<2.0
pyinstaller>=6.5,<7.0         # Phase 8: RPM bundle (separate plan)
```

- [ ] **Step 2：commit**

```bash
rtk git add requirements-dev.txt
rtk git commit -m "chore(deps): add requirements-dev.txt with test/lint/build tools

Separates dev-only packages (pytest, ruff, mypy, pyinstaller) from
production runtime so they are NOT bundled into the future RPM.

responses + freezegun pre-stage Phase 2 (HTTP mocks) and Phase 6
(scheduler time mocks)."
```

---

## Task 4：寫 verify_deps.py 腳本（先寫，下一 task 跑）

**Files:**
- Create: `scripts/verify_deps.py`

- [ ] **Step 1：建立腳本**

Use the Write tool to create `scripts/verify_deps.py` with this exact content:

```python
#!/usr/bin/env python3
"""Verify all production + dev dependencies can be imported and report versions.

Exits 0 if all packages import successfully, 1 otherwise.
Run after `pip install -r requirements.txt -r requirements-dev.txt`.
"""
from __future__ import annotations

import importlib
import sys
from typing import NamedTuple


class Pkg(NamedTuple):
    """A package to verify: distribution name, import name, optional version attr path."""
    dist: str
    module: str
    version_attr: str = "__version__"


# Production packages (must match requirements.txt)
PRODUCTION = [
    # Existing core
    Pkg("flask", "flask"),
    Pkg("pandas", "pandas"),
    Pkg("pyyaml", "yaml"),
    # Phase 1
    Pkg("rich", "rich"),
    Pkg("questionary", "questionary"),
    Pkg("click", "click"),
    Pkg("humanize", "humanize"),
    # Phase 2
    Pkg("requests", "requests"),
    Pkg("orjson", "orjson"),
    Pkg("cachetools", "cachetools"),
    # Phase 3
    Pkg("pydantic", "pydantic", "VERSION"),
    Pkg("pydantic-settings", "pydantic_settings"),
    # Phase 4
    Pkg("flask-wtf", "flask_wtf"),
    Pkg("flask-limiter", "flask_limiter"),
    Pkg("flask-talisman", "flask_talisman"),
    Pkg("flask-login", "flask_login"),
    Pkg("argon2-cffi", "argon2"),
    # Phase 5
    Pkg("openpyxl", "openpyxl"),
    Pkg("weasyprint", "weasyprint"),
    Pkg("matplotlib", "matplotlib"),
    Pkg("plotly", "plotly"),
    Pkg("pygments", "pygments"),
    # Phase 6
    Pkg("APScheduler", "apscheduler"),
    # Phase 7
    Pkg("loguru", "loguru"),
]

# Dev packages (optional — checked but not fatal if missing)
DEV = [
    Pkg("pytest", "pytest"),
    Pkg("pytest-cov", "pytest_cov"),
    Pkg("responses", "responses"),
    Pkg("freezegun", "freezegun"),
    Pkg("ruff", "ruff", ""),  # ruff has no __version__ exported; just check import
    Pkg("mypy", "mypy"),
    Pkg("build", "build"),
    Pkg("pyinstaller", "PyInstaller"),
]


def _get_version(mod, attr: str) -> str:
    """Resolve a possibly-dotted version attribute path; return 'unknown' if missing."""
    if not attr:
        return "(no version attr)"
    obj = mod
    for part in attr.split("."):
        obj = getattr(obj, part, None)
        if obj is None:
            return "unknown"
    return str(obj)


def verify(pkgs: list[Pkg], category: str, fatal: bool) -> list[str]:
    """Try to import each package; print a status line; return failed dist names."""
    print(f"\n=== {category} ({len(pkgs)} packages) ===")
    failed: list[str] = []
    for pkg in pkgs:
        try:
            mod = importlib.import_module(pkg.module)
            version = _get_version(mod, pkg.version_attr)
            print(f"  OK    {pkg.dist:<22} {version}")
        except ImportError as exc:
            mark = "FAIL" if fatal else "SKIP"
            print(f"  {mark}  {pkg.dist:<22} -- {exc}")
            if fatal:
                failed.append(pkg.dist)
    return failed


def main() -> int:
    print("illumio_ops dependency baseline verification")
    print(f"Python: {sys.version}")
    failed = verify(PRODUCTION, "Production", fatal=True)
    verify(DEV, "Development (non-fatal)", fatal=False)
    if failed:
        print(f"\nFAILED: {len(failed)} production package(s) missing: {', '.join(failed)}")
        print("Run: pip install -r requirements.txt")
        return 1
    print("\nAll production packages imported successfully.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2：commit**

```bash
rtk git add scripts/verify_deps.py
rtk git commit -m "chore(deps): add verify_deps.py to import-test all packages

Imports every package listed in requirements.txt + requirements-dev.txt
and prints version. Exits 1 if any production package fails to import.

Used as smoke test by tests/test_dependency_baseline.py and as a
manual sanity check after pip install."
```

---

## Task 5：在乾淨 venv 安裝並跑 verify_deps

**Files:**
- (no file changes; verification only)

- [ ] **Step 1：建立乾淨 venv**

```bash
cd D:/OneDrive/RD/illumio_ops
python -m venv .venv-phase0
```

Expected: `.venv-phase0/` directory created.

- [ ] **Step 2：啟動 venv 並升級 pip**

Windows PowerShell:
```bash
.venv-phase0/Scripts/Activate.ps1
python -m pip install --upgrade pip
```

Expected: pip upgraded; prompt prefixed `(.venv-phase0)`.

- [ ] **Step 3：安裝生產 + dev 依賴**

```bash
rtk pip install -r requirements.txt -r requirements-dev.txt
```

Expected: all packages installed without error. weasyprint may need GTK on Windows — see troubleshooting below.

**Windows weasyprint 注意**：weasyprint 在 Windows 需要 GTK runtime（提供 cairo/pango）。若 install 時 `import weasyprint` 失敗，下載並安裝 GTK3 runtime: `https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer/releases`，重啟 shell。**Linux RPM 環境會內建** cairo/pango，無此問題；本步驟在 Windows dev 環境失敗時不阻塞 Phase 0，記入 Status.md 即可。

- [ ] **Step 4：執行 verify_deps**

```bash
python scripts/verify_deps.py
```

Expected output（最後一行）：
```
All production packages imported successfully.
```
Exit code 0。若 weasyprint 在 Windows 失敗，記下並繼續。

- [ ] **Step 5：跑既有測試套件（必須全綠）**

```bash
rtk pytest tests/ -v
```

Expected: 127+ tests pass, 0 failures. 任何 regression 都必須在 commit 前修復（理論上不會有，因為沒改業務程式碼）。

- [ ] **Step 6：（無 commit — 此 task 純驗證）**

確認 `git status` 無新增/修改檔。

---

## Task 6：寫 pytest gate `tests/test_dependency_baseline.py`

**Files:**
- Create: `tests/test_dependency_baseline.py`

- [ ] **Step 1：寫測試**

Use the Write tool to create `tests/test_dependency_baseline.py` with this exact content:

```python
"""CI gate: ensure all production dependencies are installed and importable.

Runs scripts/verify_deps.py as a subprocess and asserts exit code 0.
Failure means requirements.txt is out of sync with reality.
"""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
VERIFY_SCRIPT = REPO_ROOT / "scripts" / "verify_deps.py"


def test_verify_deps_script_exists():
    """Phase 0 deliverable: scripts/verify_deps.py must exist."""
    assert VERIFY_SCRIPT.is_file(), f"missing {VERIFY_SCRIPT}"


def test_all_production_packages_importable():
    """All packages in requirements.txt must import in the current environment.

    This is the CI gate — if a developer adds a package to requirements.txt
    but forgets to add it to verify_deps.PRODUCTION, this test still catches
    runtime-missing packages because verify_deps tries to import each one.
    """
    result = subprocess.run(
        [sys.executable, str(VERIFY_SCRIPT)],
        capture_output=True,
        text=True,
        timeout=60,
    )
    if result.returncode != 0:
        # Surface the script's stdout so the failure is actionable.
        msg = (
            f"verify_deps.py exited {result.returncode}\n"
            f"--- stdout ---\n{result.stdout}\n"
            f"--- stderr ---\n{result.stderr}"
        )
        raise AssertionError(msg)


def test_requirements_txt_has_no_unpinned_packages():
    """Every line in requirements.txt that names a package must have a version constraint."""
    req_file = REPO_ROOT / "requirements.txt"
    assert req_file.is_file()
    offenders: list[str] = []
    for raw in req_file.read_text(encoding="utf-8").splitlines():
        line = raw.strip()
        if not line or line.startswith("#"):
            continue
        # A pinned line contains one of: ==, >=, <=, ~=, >, <, !=
        if not any(op in line for op in ("==", ">=", "<=", "~=", ">", "<", "!=")):
            offenders.append(line)
    assert not offenders, f"unpinned packages: {offenders}"
```

- [ ] **Step 2：跑這支新測試**

```bash
rtk pytest tests/test_dependency_baseline.py -v
```

Expected: 3 tests pass.

- [ ] **Step 3：跑全部測試確認沒打壞別的**

```bash
rtk pytest tests/ -v
```

Expected: 130+ tests pass (127 existing + 3 new), 0 failures.

- [ ] **Step 4：commit**

```bash
rtk git add tests/test_dependency_baseline.py
rtk git commit -m "test(deps): add Phase 0 baseline gate

3 tests:
- verify_deps.py exists
- all production packages importable (subprocess gate)
- no unpinned packages in requirements.txt

Catches drift between requirements.txt and the actual installed env."
```

---

## Task 7：更新 Status.md 與 Task.md

**Files:**
- Modify: `Status.md`
- Modify: `Task.md`

- [ ] **Step 1：在 Status.md 加 v3.4.0-deps 段落**

Read current `Status.md` to find the right insertion point. Then use the Edit tool to add this block right after the `**Version:**` header line:

```markdown
**Version:** v3.4.0-deps  
**Branch:** upgrade/phase-0-deps  
**Phase:** 0 of 9 — Dependency baseline complete (see docs/superpowers/plans/2026-04-18-upgrade-roadmap.md)
```

(Replace the previous `**Version:** v3.2.0` line — the actual current version per [Task.md](Task.md) memory is v3.3.0; verify current value before editing.)

- [ ] **Step 2：在 Status.md 的「Dependency Status」表格替換內容**

Locate the existing table titled `### Dependency Status`. Replace the table body with:

```markdown
| Package | Phase | Pinned? |
|---|---|---|
| flask | existing | ✓ >=3.0,<4.0 |
| pandas | existing (mandatory) | ✓ >=2.0,<3.0 |
| pyyaml | existing (mandatory) | ✓ >=6.0,<7.0 |
| rich, questionary, click, humanize | Phase 1 | ✓ |
| requests, orjson, cachetools | Phase 2 | ✓ |
| pydantic, pydantic-settings | Phase 3 | ✓ |
| flask-wtf, flask-limiter, flask-talisman, flask-login, argon2-cffi | Phase 4 | ✓ |
| openpyxl, weasyprint, matplotlib, plotly, pygments | Phase 5 | ✓ |
| APScheduler | Phase 6 | ✓ |
| loguru | Phase 7 | ✓ |
| pytest, ruff, mypy, build, pyinstaller, responses, freezegun (dev) | dev-only | ✓ in requirements-dev.txt |
```

- [ ] **Step 3：在 Task.md 加新段落**

Add at the top of Task.md (after the existing `**As of:**` header), this new section:

```markdown
---

## Phase 0: Dependency Baseline ✅ DONE (2026-04-18)

- [x] **Phase 0**: Pin all roadmap packages
  - `requirements.txt`: 21 production packages pinned (flask + pandas + pyyaml +
    18 new across Phase 1-7)
  - `requirements-dev.txt`: 7 dev-only packages
  - `scripts/verify_deps.py`: import-test smoke script
  - `tests/test_dependency_baseline.py`: 3-test CI gate
  - All 130+ tests pass; clean venv install verified
```

- [ ] **Step 4：commit**

```bash
rtk git add Status.md Task.md
rtk git commit -m "docs: record Phase 0 completion in Status.md + Task.md"
```

---

## Task 8：更新 mem0 + 收尾

**Files:**
- (no file changes)

- [ ] **Step 1：把 Phase 0 完成寫進 mem0**

Use the mem0 add_memory tool with this content:

```
illumio_ops Phase 0 (dependency baseline) shipped 2026-04-18 on branch
upgrade/phase-0-deps. Pinned 21 production + 7 dev packages including
new packages for upcoming Phases 1-7: rich/questionary/click/humanize
(CLI), requests/orjson/cachetools (HTTP), pydantic+pydantic-settings
(Settings), flask-wtf/flask-limiter/flask-talisman/flask-login/argon2-cffi
(Web security), openpyxl/weasyprint/matplotlib/plotly/pygments (reports),
APScheduler (scheduler), loguru (logging). pandas + pyyaml promoted from
optional to mandatory. requirements-dev.txt added for pytest/ruff/mypy/
pyinstaller/responses/freezegun. tests/test_dependency_baseline.py is the
CI gate. All 130+ tests pass.
```

- [ ] **Step 2：squash merge 到 main 並 tag**

```bash
rtk git checkout main
rtk git merge --squash upgrade/phase-0-deps
rtk git commit -m "feat(deps): Phase 0 dependency baseline (v3.4.0-deps)

Pin 21 production + 7 dev packages for upgrade roadmap Phase 1-7.
pandas + pyyaml promoted to mandatory.
Add scripts/verify_deps.py + tests/test_dependency_baseline.py CI gate.

See docs/superpowers/plans/2026-04-18-upgrade-roadmap.md
See docs/superpowers/plans/2026-04-18-phase-0-deps.md"
rtk git tag v3.4.0-deps
```

- [ ] **Step 3：(optional) push 與 tag**

```bash
rtk git push origin main
rtk git push origin v3.4.0-deps
```

(Skip if local-only workflow.)

- [ ] **Step 4：keep or delete the branch**

```bash
# Keep for audit (recommended for one release cycle):
rtk git branch -d upgrade/phase-0-deps  # local
# Or hard-delete if confident:
rtk git branch -D upgrade/phase-0-deps
```

---

## Phase 0 完成驗收清單

完成所有 Task 後，逐項確認：

- [ ] `requirements.txt` 包含 21 個生產套件，全部 pin
- [ ] `requirements-dev.txt` 包含 7 個 dev 套件，全部 pin
- [ ] `scripts/verify_deps.py` 在乾淨 venv 跑 exit 0
- [ ] `tests/test_dependency_baseline.py` 3 測試通過
- [ ] `pytest tests/` 全部 130+ 測試通過
- [ ] `python illumio_ops.py` 可進入主選單（業務行為未變）
- [ ] `python illumio_ops.py --gui` 可啟動 Web GUI（業務行為未變）
- [ ] `Status.md` 已標 v3.4.0-deps + Phase 0 完成
- [ ] `Task.md` 已加 Phase 0 完成記錄
- [ ] mem0 已記錄 Phase 0 完成
- [ ] main 有 squash merge commit + `v3.4.0-deps` tag

---

## 後續：可平行啟動 Wave A

Phase 0 merge 後，依路線圖 Wave A 三條 branch 可立即同時開：
- `upgrade/phase-1-cli-rich`
- `upgrade/phase-2-http-requests`
- `upgrade/phase-3-settings-pydantic`

各自獨立、互不阻塞。建議先寫 Phase 1 詳細計畫（最高使用者可見價值），再寫 Phase 2、3。
