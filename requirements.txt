# Core: Zero external dependencies (Python stdlib only)
# CLI and daemon modes work without any pip install.

# Optional: Web GUI mode (--gui)
flask>=3.0,<4.0

# Optional: Report generation (--report, menu items 12-14)
# pandas  — data analysis; available in RHEL AppStream / standard repos
# pyyaml  — report_config.yaml; available in RHEL AppStream (python3-pyyaml)
# (No openpyxl required — reports export to HTML and CSV ZIP, stdlib only)
# pandas>=2.0,<3.0
# pyyaml>=6.0,<7.0
