# illumio_ops production runtime dependencies.
# For offline bundle packaging: use requirements-offline.txt.
# Pinned to major version ranges; minor/patch upgrades allowed within range.
#
# Categories follow the upgrade roadmap phases:
#   Phase 1 — CLI UX:          rich, questionary, click, humanize
#   Phase 2 — HTTP client:     requests, orjson, cachetools
#   Phase 3 — Settings:        pydantic
#   Phase 4 — Web GUI security: flask-wtf, flask-limiter, flask-talisman, flask-login
#   Phase 5 — Reports:         openpyxl, reportlab, matplotlib, plotly, pygments
#   Phase 6 — Scheduler:       APScheduler, SQLAlchemy
#   Phase 7 — Logging:         loguru
#   Existing (mandatory):      flask, pandas, pyyaml, cheroot

# ── Existing core (Web GUI + reports) ────────────────────────────────────────
flask>=3.0,<4.0
pandas>=2.0,<4.0
pyyaml>=6.0,<7.0
cheroot>=10.0,<12.0

# ── Phase 1: CLI UX ──────────────────────────────────────────────────────────
rich>=13.7,<16.0
questionary>=2.0,<3.0
click>=8.1,<9.0
humanize>=4.9,<6.0

# ── Phase 2: HTTP client ─────────────────────────────────────────────────────
requests>=2.31,<3.0
orjson>=3.9,<4.0
cachetools>=5.3,<8.0

# ── Phase 3: Settings validation ─────────────────────────────────────────────
pydantic>=2.6,<3.0
# pydantic-settings removed: never imported anywhere in the codebase

# ── Phase 4: Web GUI security ────────────────────────────────────────────────
flask-wtf>=1.2,<2.0
flask-limiter>=3.5,<5.0
flask-talisman>=1.1,<2.0
flask-login>=0.6,<0.7
argon2-cffi>=23.1,<25.0
cryptography>=42.0,<46.0     # Phase 2: ECDSA cert generation (TLS)

# ── Phase 5: Reports (Excel + PDF + interactive charts) ──────────────────────
openpyxl>=3.1,<4.0
reportlab>=4.0,<5.0
matplotlib>=3.8,<4.0
plotly>=5.20,<7.0
pygments>=2.17,<3.0

# ── Phase 6: Scheduler ───────────────────────────────────────────────────────
APScheduler>=3.10,<4.0
SQLAlchemy>=2.0,<3.0      # required when pce_cache.enabled or scheduler.persist=true

# ── Phase 7: Logging ─────────────────────────────────────────────────────────
loguru>=0.7,<0.8
