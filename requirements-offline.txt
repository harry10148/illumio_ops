# illumio_ops offline bundle dependencies.
# Derived from requirements.txt; ReportLab PDF export included (pure-Python, no native deps).
# HTML/XLSX/CSV/charts work on both online and offline builds.
#
# Build command (Linux/WSL):
#   pip download -r requirements-offline.txt \
#       --platform manylinux_2_17_x86_64 --python-version 3.12 \
#       --only-binary=:all: -d dist/wheels/linux/
#
#   pip download -r requirements-offline.txt \
#       --platform win_amd64 --python-version 3.12 \
#       --only-binary=:all: -d dist/wheels/windows/
#
# Note: transitive binary dependencies (numpy, pillow, contourpy, etc.) are
# automatically included by pip download.

# ── Existing core (Web GUI + reports) ────────────────────────────────────────
flask>=3.0,<4.0
pandas>=2.0,<4.0
pyyaml>=6.0,<7.0
cheroot>=8.6,<12.0

# ── Phase 1: CLI UX ──────────────────────────────────────────────────────────
rich>=13.7,<16.0
questionary>=2.0,<3.0
click>=8.1,<9.0
humanize>=4.9,<6.0

# ── Phase 2: HTTP client ─────────────────────────────────────────────────────
requests>=2.31,<3.0
orjson>=3.9,<4.0
cachetools>=5.3,<8.0

# ── Phase 3: Settings validation ─────────────────────────────────────────────
pydantic>=2.6,<3.0

# ── Phase 4: Web GUI security ────────────────────────────────────────────────
flask-wtf>=1.2,<2.0
flask-limiter>=3.5,<5.0
flask-talisman>=1.1,<2.0
flask-login>=0.6,<0.7

# ── Phase 5: Reports (Excel + PDF + interactive charts) ──────────────────────
openpyxl>=3.1,<4.0
reportlab>=4.0,<5.0
matplotlib>=3.8,<4.0
plotly>=5.20,<7.0
pygments>=2.17,<3.0

# ── Phase 6: Scheduler ───────────────────────────────────────────────────────
APScheduler>=3.10,<4.0
SQLAlchemy>=2.0,<3.0

# ── Phase 7: Logging ─────────────────────────────────────────────────────────
loguru>=0.7,<0.8
